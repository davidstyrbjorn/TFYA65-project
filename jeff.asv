[image, map] = imread("triangle.png");
org_image = image;

fig = figure;

for x = 1:size(image, 2)
    
    image(:,x,1) = 100;
    image(:,x,2) = 0;
    image(:,x,3) = 0;
    
    imshow(image(:, :, :), 'InitialMagnification', 900);
    
    image = org_image;
    
    pause(0.5);
      
  end